[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "itchy"
version = "0.3.2"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["src/itchy"]
