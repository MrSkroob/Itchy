[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "itchy"
version = "0.1.0"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["src/itchy"]

[tool.hatch.build.targets.wheel.force-include]
"src/itchy/assets" = "itchy/assets"