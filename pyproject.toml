[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "itchy"
version = "0.4.0"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["src/itchy"]
