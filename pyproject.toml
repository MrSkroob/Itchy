[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "itch-compiler"
version = "0.1.0"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["src/itch"]

[tool.hatch.build.targets.wheel.force-include]
"src/itch/assets" = "itch/assets"