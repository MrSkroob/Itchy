[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "itchy"
version = "0.4.2"
requires-python = ">=3.12"

[project.scripts]
itchy = "itchy.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/itchy"]
