<program> ::= {<vardefstat>} <chunk> <EOF> 
<chunk> ::= {<stat> {<StatementSeparator>}}
<stat> ::= <whilestat> | <ifstat> | <forstat> | <functionstat> | <eventstat> | <varassignstat> | <functioncall> | <foreverstat> | <laststat>

<vardefstat> ::= [<Shared>] <Type> <Symbol> <StatementSeparator>
<foreverstat> ::= <Forever> <wrap>
<whilestat> ::= <While> <equation> <wrap>
<ifstat> ::= <If> <equation> <wrap> {<ElseIf> <equation> <wrap>} [<Else> <wrap>]
<forstat> ::= <For> <Symbol> <forbody> <wrap>
// some rules can have {} and () which are good enough to determine if a statement has ended.
// others like 1 += 1 should have seperators
<functionstat> ::= <Define> [<Warp>] <function>
<eventstat> ::= <Event> <Symbol> <args> <wrap>
<varassignstat> ::= <var> <Assign> <equation> <StatementSeparator>
<functioncall> ::= <Symbol> <args>
<laststat> ::= <Return> [<varlist1>] <StatementSeparator>

<asset> ::= <AssetType> <args>

<forbody> ::= "=" <equation> <FieldSeperator> <equation> <FieldSeperator> <equation> | <In> <var>
<tableconstructor> ::= "[" [<varlist1>] "]"
<wrap> ::= "{" [<chunk>] "}"
<function> ::= <Symbol> <funcbody>
<funcbody> ::= "(" [<paramlist>] ")" <wrap>
<slice> ::= "[" <equation> "]"
<equation> ::= <comparison>
<comparison> ::= <addition> { ("==" | ">" | "<" | <In> ) <addition> }
<addition> ::= <multiplication> { ("+" | "-") <multiplication> }
<multiplication> ::= <unary> { ("*" | "/" | "and" | "or") <unary> }
<unary> ::= ["-" | "not"] <primary>
<primary> ::= <literals> | "(" <equation> ")"
<var> ::= <Symbol> [<slice>]
<literals> ::= <functioncall> | <tableconstructor> | <var> | <Bool> | <Number> | <String> | <asset>
// <explist1> ::= <equation> {<FieldSeperator> <explist1>}
<varlist1> ::= <equation> {<FieldSeperator> <equation>} [<FieldSeperator>]
<args> ::= "(" [<varlist1>] ")"
<argtype> ::= <Symbol> <Colon> <Type>
<paramlist> ::= <argtype> {<FieldSeperator> <argtype>} [<FieldSeperator>]

// note that rules are evaluated left to right
// for performance, it's better to have non-terminators at the end. 
// note that rules with capital letters are terminators. 
